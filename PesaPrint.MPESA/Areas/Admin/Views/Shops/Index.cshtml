@model IEnumerable<PesaPrint.MPESA.Models.Shop>

@section Styles
{
    @Styles.Render("~/Content/kendo/css")
    @Styles.Render("~/Content/shared/css")

}

<h2> <i class="fa fa-tag"></i> View Shops</h2>

@using Kendo.Mvc.UI
﻿﻿@(Html.Kendo().Grid<PesaPrint.MPESA.Models.Shop>()
      .Name("grid")
      .Columns(columns =>
      {
          columns.Bound(c => c.Name);
          columns.Bound(c => c.AgentNumber);
          columns.Bound(c => c.MerchantId);

      })
      .ToolBar(toolbar =>
      {
          toolbar.Pdf();
      })
      .Pageable()
      .Filterable()
      .Scrollable()
      .DataSource(dataSource => dataSource
          .Ajax()
          .Read(read => read.Action("Shops_Read", "Shops"))
      )
)

@section scripts
{
    @Scripts.Render("~/bundles/kendo")
    @Scripts.Render("~/bundles/demo")
}
